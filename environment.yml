name: myenv
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.9
  - pip
  - pip:
    - gdown
    - streamlit
    - opencv-python
    - mediapipe
    - numpy
    - tensorflow
    - pandas
